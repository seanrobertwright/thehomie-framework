/content-engine
/TheHomie
/obsidian-ai-agent
/video-processor
/remotion-videos
.claude/skills/pptx-generator/.tmp/
/.tmp/
/tmp
mcp-config.json
master.env
.mcp.json
# .claude/settings.local.json 
__pycache__/


# Memory files now live in vault/memory/ (Obsidian vault, synced)
.claude/memory/

# Heartbeat scripts
# .claude/scripts/.env 
.claude/scripts/.venv/
.claude/scripts/heartbeat_runs.log
.claude/scripts/monthly_finance_runs.log
.claude/scripts/reflection_runs.log
.claude/scripts/weekly_runs.log
.claude/scripts/uv.lock
.claude/chat/*.log
.claude/dashboard/*.log
.claude/skills/generated/
.claude/skills/promoted/
.context/

# Memory search data (Phase 3)
.claude/data/

# User-specific tone of voice (copied from templates on setup)
.claude/tone-of-voice.md

# Phase 5: Integration credentials & tokens
.claude/scripts/integrations/google_credentials.json
.claude/scripts/integrations/google_token*.json

# Certificates and private keys
*.pem
*.key
*.p8
*.p12
.claude/scripts/certs/

# Windows artifacts
NUL
# Stray HuggingFace cache written as literal "E:/" Windows path under Linux
.claude/scripts/E:/

# Archon worktrees (embedded git repos for parallel agent work)
/.worktrees/

# Local Codex/agent generated surfaces
.agents/skills/
.codex/agents/
.codex/hooks.json
.codex/hooks/
.codex-worktrees/
.claude/worktrees/
.claude/scripts/.codex-*/
PRPs/planning/.clutch-tmp/

# Archon writes per-run research/scratch here (brain-dump-* dirs, etc.). A
# committed scratch file caused a cross-PR merge collision (2026-06-18).
# Ignoring the dir stops NEW scratch from being tracked; already-committed
# reference docs stay tracked. Sanitizer sources from git ls-files.
.claude/research/

# Locally synced vault skills; canonical copies live outside this checkout
.claude/skills/vault/
.claude/skills/vault-*/

# Stray entity_extractor output if it ever lands at repo root again
/concepts/
/connections/

# Graphify knowledge-graph output — graph.json can embed vault/doc content;
# stays local. Sanitizer sources from git ls-files, so gitignore = export-proof.
graphify-out/

# Dashboard slice (PRD-8 Phase 3)
dashboard/server/dist/
dashboard/server/node_modules/
dashboard/desktop/dist/
dashboard/desktop/node_modules/
dashboard/desktop/out/
dashboard/web/dist/
dashboard/web/node_modules/

# Per-script run logs (the existing list named specific files; these were missed)
.claude/scripts/*_runs.log
.claude/scripts/output/

# Browser-automation screenshots dumped at repo root / scripts root
/*.png
.claude/scripts/*.png

# Stray artifacts — script bugs wrote DBs/docs to literal paths. Content (if any)
# stays on disk; we just stop tracking these locations.
/data/
/undefined/
/coding-vault/
.claude/scripts/None
.claude/scheduled_tasks.lock

# Local agent scratch (not previously covered)
.archon/logs/
.claude/tmp/
.codex/artifacts/
.codex-remote-attachments/

# Installed HyperFrames skill bundle — these are symlinks into the gitignored
# .agents/skills/ install dir; tracking them would commit dangling symlinks.
.claude/skills/animejs
.claude/skills/contribute-catalog
.claude/skills/css-animations
.claude/skills/gsap
.claude/skills/hyperframes
.claude/skills/hyperframes-cli
.claude/skills/hyperframes-media
.claude/skills/hyperframes-registry
.claude/skills/lottie
.claude/skills/remotion-to-hyperframes
.claude/skills/tailwind
.claude/skills/three
.claude/skills/typegpu
.claude/skills/waapi
.claude/skills/website-to-hyperframes
skills-lock.json

# Video render output — hundreds of MB of HTML/audio/video/image binaries; stays local
.claude/video/
