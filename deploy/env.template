# ============================================================
# The Homie — Environment Variables
# Copy to .claude/scripts/.env and fill in your values.
# ============================================================

# ── Core ─────────────────────────────────────────────────────
OWNER_NAME=

# ── Telegram ─────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=

# ── Slack ────────────────────────────────────────────────────
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
CHAT_ALLOWED_USERS=

# ── Discord ──────────────────────────────────────────────────
DISCORD_BOT_TOKEN=
DISCORD_ALLOWED_GUILDS=
DISCORD_ALLOWED_USERS=

# ── Buzz signed collaboration ───────────────────────────────
BUZZ_RELAY_URL=

<REDACTED-buzz-private-key>
# Allowlisted senders default to viewer; elevate only explicit pubkey=role pairs.
BUZZ_PUBKEY_ROLES=




BUZZ_TRANSPORT=auto
BUZZ_REQUIRE_MENTION=true
BUZZ_DESKTOP_PATH=


WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_WEBHOOK_PORT=8443

# ── Health Check ─────────────────────────────────────────────
HEALTH_CHECK_PORT=8787

# ── Voice ────────────────────────────────────────────────────
OPENAI_API_KEY=
VOICE_STT_MODEL=whisper-1
VOICE_TTS_ENGINE=edge
VOICE_TTS_VOICE_EDGE=en-US-GuyNeural
VOICE_TTS_VOICE_OPENAI=alloy

# ── Google OAuth ─────────────────────────────────────────────
GOOGLE_CALENDAR_ID=

# ── Chat Engine ──────────────────────────────────────────────
CHAT_MAX_TURNS=25
CHAT_MAX_BUDGET_USD=2.0

# ── Heartbeat ────────────────────────────────────────────────
HEARTBEAT_INTERVAL_MINUTES=30
HEARTBEAT_ACTIVE_HOURS_START=08:00
HEARTBEAT_ACTIVE_HOURS_END=22:00
HEARTBEAT_TIMEZONE=America/Los_Angeles

# ── Deprecated Mission Control relay (compatibility only) ───
RELAY_WS_URL=
RELAY_AUTH_TOKEN=

# ── Deprecated Mission Control heartbeat (compatibility only)
MC_HEARTBEAT_URL=
MC_AGENT_API_KEY=

# ── Cognition Tuning ────────────────────────────────────────
# COGNITION_TEMPERATURE=0.7
# COGNITION_MAX_TOKENS=4096
# COGNITION_SYSTEM_PROMPT_OVERRIDE=
# COGNITION_PROVIDER=claude
# COGNITION_MODEL=
